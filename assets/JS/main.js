var totalAge = (90 - age);

    var totalDays = (totalAge * 365);

    var totalWeeks = (totalAge * 52);

    var totalMonths = (totalAge * 12);

    console.log("You have " +totalDays+ " days, "  +totalWeeks + " weeks, and " +totalMonths+ " months left.");